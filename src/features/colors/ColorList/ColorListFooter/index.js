import React from "react";
import TotalColor from "./TotalColor";

const ColorListFooter = () => {
  return (
    <footer className="footer">
      <TotalColor />
    </footer>
  );
};

export default ColorListFooter;
